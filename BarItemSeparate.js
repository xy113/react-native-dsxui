import React from "react";
import {View} from "react-native";

const BarItemSeparate = ({width = 14, height = 28}) => (
    <View style={{width: width, height: height}}/>
);

module.exports = BarItemSeparate;
